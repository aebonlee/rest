# 통합 개발일지 — 2026-06-01 (강의 1일차 운영 대응 종합)

- **사이트**: rest.dreamitbiz.com (AI Reboot Academy / 쉬었음 청년 AI교육 LMS)
- **GitHub**: https://github.com/aebonlee/rest · **배포**: `npm run deploy`(gh-pages, 수동)
- **스택**: React 19 + Vite 7 + TS 5.8 + Supabase(`rest_` prefix)

강의 1일차 운영 중 들어온 현장 요청들을 처리한 하루 종합 기록. 세부 기록: [세션2](2026-06-01-session2-auth-team-grades.md) · [세션3](2026-06-01-session3-grade-attendance-export.md).

---

## A. 로그인/세션 안정화 — ✅ 배포완료
- 무동작 자동 로그아웃 **10분 → 60분**, 경고 2분 전, 안내문구 동적화 (`useIdleTimeout.ts`)
- 타임아웃 시 `signOut()`으로 로컬 상태까지 정리 → "로그인된 듯 안 되는" 불일치 제거 (`AuthContext.tsx`)
- **원인 분석**: 10분 타임아웃이 시험(40분대) 중 로그아웃 → 제출이 게스트 처리되어 성적 미저장 (전유미 사례의 근본 원인)

## B. 평가 응시 미반영 자동복구 — ✅ 배포완료
- 세션 끊겨 미저장된 채점결과를, 재로그인 후 평가 페이지 재진입 시 **자동 재저장** (`Assessment.tsx`)
- 답안은 localStorage 보존 → 끊긴 학생도 복구 가능

## C. 팀구성 — 강사 참여 차단 — ✅ 배포완료
- "클릭한 학생이 팀장" 동작 유지, **강사(isAdmin)만** 팀 생성/합류 차단 + 안내문구 (`ProjectVote.tsx`)
- 이미 들어간 팀의 "팀 나가기"는 유지(잘못 들어간 팀 직접 탈퇴 가능)

## D. 동일인 통합 — 이메일 2개 가입 학생 — ✅ 배포완료
- `utils/people.ts` `groupByPerson`(전화번호>이름 키). **비파괴**(두 이메일 모두 로그인 유지)
- 적용: 수강생관리/성적/출결/대시보드/명단대조 — 한 명으로 표시·집계(성적 최고점, 출결 합산)
- 확인된 중복: 임종권(010-4052-0834), 최재영(010-9318-3978)

## E. 권한 — 백진주 rest 한정 관리자 승급 — ✅ 배포완료
- `config/admin.ts` `ADMIN_EMAILS`에 `a01094819953@gmail.com` 추가 (이 레포는 rest에만 배포 → 사이트 한정)

## F. 관리자 명단 정리 + 성적표/출결일지 다운로드 — ✅ 배포완료
- **명단 제외**: 학습평가·출석 화면에서 총괄관리자(역할) + 관리자 이메일(백진주 등) 제외 → 순수 수강생만
- **성적표**: 선수/사후평가 각 **Excel·PDF** 다운로드 (`AdminGrades`)
- **출결일지**: 일자별 **Word·PDF** 다운로드 (`AdminAttendance`)
- 유틸 `utils/exportTable.ts` — Excel(xlsx 동적import, 별도청크) / PDF(인쇄창) / Word(.doc), 모두 한글 정상
- deps: `xlsx@0.18.5` 추가

## G. 문구 수정 — ✅ 배포완료
- `/competition` LLM 카드: ChatGPT "- 유료 플랜 제공" 삭제, Claude "- 유료 플랜 1개월 제공(6월 한달)" 추가

---

## 🔧 운영자 액션 (Supabase SQL Editor) — `supabase/ops_2026-06-01.sql`
- **§1 전유미 선수평가 55점(11/20, 합격)** 수동 반영 — upsert
- **§2 팀에 잘못 들어간 강사 제거 + 빈 팀 삭제**
- **§3 백진주 성적 조회 RLS** — `rest_assessments_select`에 이메일 추가 (성적표 열람용)
- **§4 user_profiles SELECT RLS 축소(본인+관리자)** — PII 노출 차단. ✅ **실행 확인됨**(anon 조회 0건)
  - 백진주는 `signup_domain='rest.dreamitbiz.com'` 한정 분기로 rest 학생만 조회

## ⚠️ 잔여/후속
- 사용자 확인: §1·§2·§3·§4 "SQL 다 실행했다" — 백진주 성적 열람은 본인 로그인으로 최종 확인 권장
- `user_profiles` UPDATE/INSERT 정책 과개방 여부 미점검 (§4-0 쿼리로 확인 권장)
- "ChatGPT 유료 플랜 제공" 문구가 `Home.tsx`(2곳)·`Competition.tsx:47`에도 잔존 — 혜택이 Claude로 바뀌었다면 추가 수정 필요(미확정)

## 오늘 커밋
`39ca3cb`(세션·평가·팀·백진주) · `3e883df`(RLS SQL) · `14ee4bf`(동일인 통합) · `78b41be`(§3 SQL) · `d4736a1`(다운로드/명단제외) · `a6ad3df`(LLM 문구)
