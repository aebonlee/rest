# 2026-06-04 개발일지 — 성적표 보강 · 공지사항 시스템 · 학습자료 강조

오늘 작업 9개 커밋. 크게 ① 학습평가 성적표 정합성/UX, ② 공지사항 전체 기능, ③ 학습자료·명단 보완.

---

## 1. 학습평가 성적표 — 점수 누락 해결 + 넘버링
> 상세: [2026-06-04-orphan-grades-panel.md](./2026-06-04-orphan-grades-panel.md)

- **사전평가 점수 누락 진단**: 한승우·조윤서·이유민이 선수평가를 정상 응시·합격했으나
  성적표에 안 잡힘 → 원인은 **다른 dreamitbiz 사이트로 가입(signup_domain ≠ rest)**.
- **고아 점수 경고 패널**(`AdminGrades`): 수강생 목록과 매칭 안 된 평가기록을 상단에 노출해
  조용히 사라지던 점수를 드러냄. (`ab27e2c`)
- **근본 수정**: AdminRoster 방식(visited_sites 포함 조회 + 평가기록 student_id 로 프로필 보강)으로
  타 도메인 가입자도 집계되게 함. 이유민 닉네임('행복한흰이마기러기')은
  `SAME_PERSON_EMAIL_GROUPS` 에 추가해 실명 표시. (`d988a44`)
- **No. 넘버링**: 성적표 화면 표 + Excel/PDF 다운로드 맨 앞에 순번 컬럼 추가. (`3fb37d2`, `58ac0de`)

## 2. 공지사항 시스템 (신규)
관리자만 쓰던 공지를 수강생이 제대로 보고 탐색할 수 있게 전체 구축.

- **목록 페이지** `/announcements` + **상세** `/announcements/:id` 신설. 카테고리 배지(일반/중요/일정)·
  📌고정·번호 표시. (`b8c45b9`)
- **대시보드 연동**: 공지 제목 클릭→상세, '전체보기' 링크, 바로가기 카드.
- **학습관리 메뉴**에 공지사항 추가(대시보드 다음). translations ko '공지사항' / en 'Notices'.
  드롭다운 min-width 220px·균일 패딩으로 여백 정상(CSS 추가 불필요). (`f2dd520`)
- **검색 + 카테고리 필터**: 제목·내용 검색 + 카테고리 칩(건수 표시). (`6e575a8`)
- **아코디언 방식**: 목록 항목 클릭 시 페이지 이동 대신 그 자리 아래로 본문 펼침 →
  상단 '공지사항' 타이틀 유지. 상세 페이지에도 page-header 추가(대시보드 직접 진입 대비). (`6e575a8`)

## 3. 학습자료 · 명단 보완
- **학습자료**: '웹 프론트엔드 기초'를 빨간(#dc2626) 테두리·'학습추천' 배지로 강조.
  `SiteLink` 에 `accent`·`badge` 옵션 추가(featured 색상 커스텀). 설명을 HTML5/CSS3/JS/DOM/반응형
  학습 안내로 상세화. (`d2aef64`)
- **명단**: '명단외 가입' 문구를 '운영사 관리자'로 수정(해당 3명이 운영사 관리자급). (`2bab269`)

---

## 검증 / 배포
- 각 단계 `tsc -b` 통과, `npm run deploy`(gh-pages) 수동 배포 완료.
- 신규 파일: `src/pages/Announcements.tsx`, `src/pages/AnnouncementDetail.tsx`,
  `supabase/check_assessment_2026-06-04.sql`.
